# Shared environment file for OntoloGYM.
# Copy to .env and fill only the providers you use.

OPENAI_API_KEY=
OPENAI_BASE_URL=
ANTHROPIC_API_KEY=

# QA extractor defaults. config.py values are used when these are empty.
DEFAULT_LLM_MODEL=
DEFAULT_TOP_P=
DEFAULT_TEMPERATURE=
